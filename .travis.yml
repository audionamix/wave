language:
  - cpp

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
      - george-edison55-precise-backports
    packages:
      - cmake
      - cmake-data
      - g++-5
      - sox

before_script:
  - curl -O https://archive.org/download/test_wav/Untitled3.wav
  - mv Untitled3.wav test1.wav
  - soxi test1.wav

script:
  - mkdir build
  - cd build
  - cmake -DCMAKE_INSTALL_PREFIX=$PWD/install ..

  # downloading resource doesn't seem to work
  - mv ../test1.wav test_resource/test1.wav

  - make
  - ./src/wave/wave_tests
  - make install
